# TowerDefence
Small game test - codex

## Local development

```sh
npm install
npm run dev
```

This starts a local server at `http://localhost:5173` and bundles the game into `dist/main.js` using the local `three` and `OBJLoader` dependencies, so it runs without hitting a CDN (including offline).
